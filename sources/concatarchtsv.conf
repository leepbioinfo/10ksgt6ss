# Configuration file generated by /home/linuxbrew/git/rotifer/bin/tfilter (PID: 68010)

# Main section
filter = $. > 1 || !exists $H{c}
filter = $H{c} = 1; my @a = split(/\./, $H{"current_file"}); push(@F, /domain/ ? "source" : $a[-4]); 1
